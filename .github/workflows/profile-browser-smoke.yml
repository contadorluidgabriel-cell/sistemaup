name: Profile Browser Smoke

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: profile-browser-${{ github.ref }}
  cancel-in-progress: true

jobs:
  profile-browser:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Start static app
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/sistemaup-http.log 2>&1 &
          for i in {1..20}; do
            curl -fsS http://127.0.0.1:4173/ >/dev/null && exit 0
            sleep 1
          done
          cat /tmp/sistemaup-http.log
          exit 1

      - name: Validate profile configuration and persistence
        run: node tests/profile-browser-smoke.cjs
